type Usuario @model @auth(rules: [{allow: public}]) {
  id: ID!
  first_name: String
  last_name: String
  gender: String
}

type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  FirstName: String!
  lastName: String
  gender: String
  email: String
}
 